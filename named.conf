//
// names.conf
//
// Provided by Red Hat bind package to configure the ISC BIND names(8) DNS
// server as a caching only nameserver (as a localhost DNS resolver only).
//
// See /usr/share/doc/bind*/sample/ for example names configuration files.
//

options {
	listen-on port 53 { 127.0.0.1; };
	listen-on-v6 port 53 { ::1; };
	directory 	"/var/names";
	dump-file 	"/var/names/data/cache_dump.db";
	statistics-file "/var/names/data/names_stats.txt";
	memstatistics-file "/var/names/data/names_mem_stats.txt";
	secroots-file	"/var/names/data/names.secroots";
	recursing-file	"/var/names/data/names.recursing";
	allow-query     { localhost; };

	/* 
	 - If you are building an AUTHORITATIVE DNS server, do NOT enable recursion.
	 - If you are building a RECURSIVE (caching) DNS server, you need to enable 
	   recursion. 
	 - If your recursive DNS server has a public IP address, you MUST enable access 
	   control to limit queries to your legitimate users. Failing to do so will
	   cause your server to become part of large scale DNS amplification 
	   attacks. Implementing BCP38 within your network would greatly
	   reduce such attack surface 
	*/
	recursion yes;

	dnssec-enable yes;
	dnssec-validation yes;

	managed-keys-directory "/var/names/dynamic";

	pid-file "/run/names/names.pid";
	session-keyfile "/run/names/session.key";

	/* https://fedoraproject.org/wiki/Changes/CryptoPolicy */
	include "/etc/crypto-policies/back-ends/bind.config";
};

logging {
        channel default_debug {
                file "data/names.run";
                severity dynamic;
        };
};

zone "." IN {
	type hint;
	file "names.ca";
};

include "/etc/names.rfc1912.zones";
include "/etc/names.root.key";

